# Runtime dependencies for the Django app (AthensMT).
# The repo-root requirements.txt is stale (see CLAUDE.md) — this is the real list.
Django>=5.0
pandas
numpy
openai
Pillow
pypdf
cryptography  # pypdf needs this to read AES-encrypted PDFs (common for "protected" exports)
