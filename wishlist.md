I want a gallery of images generated cross projects and per projects as well. with retry (individual and multiple). load the same config you used (prompts and all) but regenerate only the selected.

the Image generation now has only the same live logs once done, add a project gallery link to home under respective sections. also provision to download the entire files as zip. 

Image upscaling after Generation (later)

