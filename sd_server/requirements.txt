# ODT Stable Diffusion server dependencies.
# Install these ONLY where the server runs (GPU box or your Mac) — the Django
# app never imports torch/diffusers.
#
# For CUDA, install the torch build matching your CUDA version from
# https://pytorch.org/get-started/locally/ before/instead of the plain torch below.
fastapi
uvicorn[standard]
torch
diffusers>=0.30
transformers
accelerate
peft>=0.17.0
huggingface_hub
safetensors
pillow
psutil
sentencepiece
protobuf
python-dotenv

# Optional: live NVIDIA GPU utilization/temperature for the metrics panel.
# Skipped gracefully (util/temp report as unavailable) if not installed or
# there's no NVIDIA GPU — irrelevant on Apple Silicon / CPU-only setups.
nvidia-ml-py; sys_platform == "linux" or sys_platform == "win32"
